.planned_dive.snippet
  %h3=link_to "#{"Planned #{planned_dive.event_type} - " if homepage}#{planned_dive.destination}", planned_dive_path(planned_dive)
    
  %h5.byline=byline(planned_dive)
  
  .markdown
    =markdown(planned_dive.description, truncate)
        
  .read_more=link_to "…Find out more and sign up", planned_dive_path(planned_dive)