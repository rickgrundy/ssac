=content_for :title, "A Photo from #{@photo.trip_report.destination}"

.row
  .twelvecol.last
    %h5.byline=@photo.byline
    .photo
      %p=link_to image_tag(@photo.image.url(:full)), trip_report_path(@photo.trip_report) 
      %p=link_to "Back to the #{@photo.trip_report.destination} Trip Report", trip_report_path(@photo.trip_report)