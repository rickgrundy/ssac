-if record.errors.any?
  #error_explanation
    %h2
      =pluralize(record.errors.count, "error") 
      prevented this
      =record.class.name.underscore.humanize.downcase
      from being saved:
    %ul  
    -record.errors.full_messages.each do |msg|
      %li=msg